const markBMI = 24.194608809993426
const jhonBMI  = 27.309968138370508

if (markBMI > jhonBMI) {
    console.log("Mark's BMI (" + markBMI + ") is higher than Jhon's BMI (" + jhonBMI + ")");
} else {
    console.log(`Jhon's BMI (${jhonBMI}) is higher than Mark's BMI (${markBMI})`); 
}