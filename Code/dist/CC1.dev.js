"use strict";

// Data 1
var xMass = 92;
var xHeight = 1.95;
xMass = xHeight = xMass / (xHeight * xHeight);
var nellMass = 78;
var nellHeight = 1.69; // --------------------------------

var honeyMassHeightBMI = xMass / xHeight * xHeight;
console.log(honeyMassHeightBMI);
var nellMassHeightBMI = nellMass / (nellHeight * nellHeight);
console.log(nellMassHeightBMI); // --------------------------------

var honeyMassHeighterBMI = false;
var nellMassHeighterBMI = true; // --------------------------------
// Data 2

var xJhonMass = 85;
var xJhonHeight = 1.76;
xMass = xJhonHeight = xJhonMass / (xJhonHeight * xJhonHeight);
var markMass = 95;
var markHeight = 1.89; // --------------------------------

var jhonMassHeightBMI = xJhonMass / xJhonHeight * xJhonHeight;
console.log(jhonMassHeightBMI);
var markMassHeightBMI = markMass / (markHeight * markHeight);
console.log(markMassHeightBMI); // --------------------------------

var jhonMassHeighterBMI = true;
var markMassHeighterBMI = false; // --------------------------------